# macOS
.DS_Store

# Node / Wrangler (local tools, never committed)
node_modules/
.wrangler/
.dev.vars*
.env*

# Editors
.vscode/
.idea/

# His Anki workspace: the deck he bought (222 MB), the built Mac app and the extracted copy.
# The deck is Eidetic's and licensed to one person — it lives in his own database, never in here.
AnkiCardsTryOne/
