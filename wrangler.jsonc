// Cloudflare Workers config. The site is plain static files in ./public — no Worker script, no build step.
// "name" must match the Worker's name in the Cloudflare dashboard, or Workers Builds refuses to deploy.
{
  "name": "arabic",
  "compatibility_date": "2026-09-21",
  "assets": {
    "directory": "./public"
  }
}
