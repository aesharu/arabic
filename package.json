{
  "name": "najdi",
  "private": true,
  "type": "module",
  "description": "Najdi Arabic study site — plain HTML/CSS/JS, served as static files by Cloudflare Workers.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "dev": "python3 -m http.server 8000 --directory public",
    "vocab": "node scripts/build-vocab.mjs"
  }
}
